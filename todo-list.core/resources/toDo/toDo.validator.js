const validator = require('validator');
const { update } = require('./users.controller');


const invalidFieldMessages = {}

module.exports = {

    create(data) {},

    update(data) {},

    login(data) {}
}